{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.user_register'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.user_register'|trans }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">
        {{ 'action.save'|trans }}
    </button>
    {{ form_end(form) }}

    <a href="{{ url('book_index') }}" title="{{ 'action.task_list'|trans }}">
        {{ 'action.task_list'|trans }}
    </a>

{% endblock %}
